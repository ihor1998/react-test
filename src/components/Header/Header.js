function Header() {
  return (
    <div className="selection-container">
      <select>
        <option>Факультет</option>
      </select>
      <select>
        <option>Курс</option>
      </select>

      <div>&times</div>
    </div>
  );
}

export default Header;
